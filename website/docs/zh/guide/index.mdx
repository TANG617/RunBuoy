---
description: 使用 Agent 提示词或手动流程，在 macOS 或 Linux 上安装并验证 RunBuoy Skill 与 CLI。
---

# 快速开始

import { Tab, Tabs } from '@rspress/core/theme';

RunBuoy Skill 与 CLI 当前都可以安装。推荐先把完整提示词交给支持 Skill 的 Agent；希望逐条控制命令时，也可以使用下面的手动流程。完整的手机配对需要一台安装了 RunBuoy iOS 18+ App 的 iPhone。

<div id="agent-install"></div>

## 使用 Agent 安装（推荐）

:::tip 一条提示词安装 Skill + CLI
Agent 会先安装 RunBuoy Skill，再读取 Skill 自带的安全规则安装和验证 CLI。涉及 `sudo`、系统包管理器或 curl 安装器时，它必须先说明命令并等待确认。
:::

复制下面整段提示词并粘贴给支持 Skill 的 Agent：

```text
请帮我安装并验证 RunBuoy：

1. 使用你原生支持的 Skill 安装机制，从
https://github.com/TANG617/RunBuoy/tree/main/skills/runbuoy
安装 RunBuoy Skill，完整保留 SKILL.md、agents/openai.yaml 和 references/ 目录，不要猜测安装路径。

2. 安装后读取该 Skill 的 references/installation.md，并按照其中的安全规则安装 RunBuoy CLI。先检查 command -v runbuoy；如果缺失且 uv 已可用，执行：
uv tool install --python 3.12 runbuoy

3. 如果缺少 uv、tmux，或者需要 sudo、系统包管理器或 curl 安装器，请先说明将执行的命令并等待我的确认。

4. 最后运行：
runbuoy --version
runbuoy doctor --json
runbuoy capabilities --json

只汇报 Skill 是否能以 $runbuoy 被发现、CLI 版本、local_ready 和 delivery 状态。不要启动配对、Demo、被监控命令，也不要上传日志。
```

这个流程只负责安装与验证，不会配对手机或启动任务。[了解 Agent 安装提示词的设计与使用方式](/guide/agent-skill)。

<div id="manual-install"></div>

## 手动安装 CLI

:::tip 手动安装 · 当前可用
下面的 uv 流程是公开、可执行的安装方式，支持 macOS 与 Linux。
:::

### 1. 安装系统依赖与 uv

<Tabs>
<Tab label="macOS">

```bash
brew install tmux uv
uv tool update-shell
```

</Tab>
<Tab label="Debian / Ubuntu">

```bash
sudo apt update
sudo apt install tmux
curl -LsSf https://astral.sh/uv/install.sh | sh
uv tool update-shell
```

</Tab>
<Tab label="Fedora">

```bash
sudo dnf install tmux
curl -LsSf https://astral.sh/uv/install.sh | sh
uv tool update-shell
```

</Tab>
<Tab label="Arch Linux">

```bash
sudo pacman -S tmux
curl -LsSf https://astral.sh/uv/install.sh | sh
uv tool update-shell
```

</Tab>
</Tabs>

重启终端后继续。uv 会自动下载并管理所需的 Python 3.12；`tmux` 是必须通过系统包管理器安装的依赖。

### 2. 安装 CLI

```bash
uv tool install --python 3.12 runbuoy
```

根据实际使用的 Shell 选择一个补全命令：

<Tabs>
<Tab label="Bash">

```bash
runbuoy completion install bash
```

</Tab>
<Tab label="Zsh">

```bash
runbuoy completion install zsh
```

</Tab>
<Tab label="Fish">

```bash
runbuoy completion install fish
```

</Tab>
</Tabs>

### 3. 验证安装

```bash
runbuoy --version
runbuoy doctor --json
runbuoy capabilities --json
```

doctor schema v2 的 `local_ready=true` 表示平台、Python、tmux 与本地存储已满足执行条件。`delivery.ready` 是独立状态；尚未配对或 Server 不可达时，本地 Run 仍完整可用。

## 可选：在 Python 项目中使用 API

`uv tool` 创建的 CLI 环境与项目 Python 环境相互隔离。如果代码需要 `from runbuoy import progress` 等 API，请在项目根目录另行声明依赖：

```bash
cd my-project
uv add --optional runbuoy runbuoy
uv sync --extra runbuoy
```

这是正常的双环境安装，不是重复配置。SDK 调用必须发生在 RunBuoy 启动的目标进程树内；详见[进度模式](/guide/progress)。

## 已经手动安装了 CLI？

仍然可以在任何时候使用上面的 Agent 提示词补装 Skill。Agent 会先运行 `command -v runbuoy`，检测到现有 CLI 后不会重复安装，只会继续验证环境。

## 已有可用 iOS App？继续配对

只有在你已经获得并安装 RunBuoy iOS 18+ App 后，才继续以下步骤：

```bash
runbuoy device pair
runbuoy run -- python3 experiment.py
```

扫描一次性配对码后，命令仍在电脑上运行；iPhone 只显示经过筛选的状态、进度、时间与结果。现有 Demo 命令可用于独立验证，但不是安装必经步骤。

## 下一步

- [完整安装、升级与卸载说明](/guide/install)
- [配对更多电脑](/guide/pairing)
- [运行命令与发送通知](/guide/run)
- [配置真实进度](/guide/progress)
