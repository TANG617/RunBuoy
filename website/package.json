{
  "name": "@runbuoy/website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "rspress dev",
    "build": "rspress build",
    "preview": "rspress preview",
    "typecheck": "tsc --noEmit",
    "check:site": "node scripts/check-site.mjs",
    "check": "npm run typecheck && npm run build && npm run check:site"
  },
  "dependencies": {
    "@phosphor-icons/react": "^2.1.10",
    "@rspress/core": "2.0.18"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@types/react": "^19.0.0",
    "typescript": "5.9.3"
  }
}
