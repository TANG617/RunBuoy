[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "runbuoy-sdk"
version = "0.1.0"
description = "Structured local progress client for RunBuoy"
requires-python = ">=3.12"

[tool.hatch.build.targets.wheel]
packages = ["src/runbuoy"]
